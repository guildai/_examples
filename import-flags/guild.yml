model: script
operations:
  prepare:
    main: logistic_regression --data_dir . --prepare
  train:
    main: logistic_regression --data_dir data
    flags:
      $import:
        - learning_rate
        - training_epochs
        - batch_size
        - prepare
