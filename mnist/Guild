#-*-conf-*-

[project]

name            = MNIST
runroot         = runs

[model "intro"]

runtime         = tensorflow
train           = intro
prepare         = intro --prepare
train_requires  = ./data
evaluate        = intro --evaluate

[model "expert"]

runtime         = tensorflow
train           = expert
prepare         = expert --prepare
train_requires  = ./data
evaluate        = expert --evaluate

[model "tflearn-dnn"]

runtime         = tflearn
train           = tflearn_dnn

[model "tflearn-cnn"]

runtime         = tflearn
train           = tflearn_cnn

[flags]

datadir         = ./data
rundir          = $RUNDIR
batch-size      = 100
epochs          = 2

[view]

fields           = validation-accuracy train-accuracy steps time

series-b         = loss accuracy op-cpu-percent op-memory \
                   gpu-percent gpu-memory

compare          = validation-accuracy train-accuracy batch-size-flag \
                   steps time

[resource "samples"]

runtime         = tensorflow
prepare         = prepare_samples --sample-dir samples

[flags "samples"]

sample-count    = 100
